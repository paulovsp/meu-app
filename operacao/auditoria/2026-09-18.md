# Auditoria — 18/09/2026 · AMARELO

Veredito: uma diferença, sem explicação conhecida — incidente #7 (aviso). O resto bateu com o esperado: políticas e grants idênticos ao snapshot, nenhum segredo real no repositório, deploy consistente em todas as 34 funções, nenhuma função de boot quebrada.

## 1. Políticas e grants
- Conferido: `op-agente` ação `politicas` (179 linhas: `pg_policies` de `public`/`storage` + `execute` de `anon`/`authenticated`) contra `supabase/checks/politicas-esperadas.json`, comparado linha a linha por (tipo, objeto, nome), incluindo `detalhe`/`regra`.
- Resultado: idêntico. 0 só no banco, 0 só no snapshot, 0 diferentes em `detalhe`/`regra`. Total 179/179.
- Evidência: diff programático (chave tipo+objeto+nome) entre a resposta da ação `politicas` e o snapshot — nenhuma linha sobrou de nenhum dos dois lados.

## 2. Anomalias (`op-agente` ação `auditoria`)
| item | valor | situação |
|---|---|---|
| tabelas_sem_rls | 0 | ok |
| funcoes_definer_para_anon | 0 | ok |
| contas_cortesia_longa | 0 | ok |
| contas_gratuita_indicacao | 0 | ok |
| contas_credito_muito_negativo | 0 | ok |
| contas_demonstracao | 1 | explicado — cortesia de propósito, é a conta de demonstração |
| convites_pendentes | 13 | explicado — testadoras convidadas que ainda não se cadastraram |
| eventos_nao_tratados_7d | 0 | ok |
| incidentes_abertos_mais_de_7d | 0 | ok |
| **contas_ativas_sem_preapproval** | **1** | **sem explicação conhecida → incidente #7 (aviso)** |

Evidência: `contas_ativas_sem_preapproval` conta perfis com `assinatura_status = 'ativa'`, `mp_preapproval_id` nulo e `conta_demonstracao = false`. O único caminho que grava `assinatura_status: 'ativa'` (`mercadopago-webhook` → `aplicarAssinaturaPorId`) sempre grava `mp_preapproval_id` na mesma chamada — não há combinação legítima documentada de conta ativa sem preapproval. Sem dado de usuária neste relatório; o perfil específico precisa ser conferido direto no painel do Supabase.

## 3. Segredos no repositório
- Conferido: `git grep -nE "(sk_live|sb_secret|service_role|whsec_|APP_USR-|eyJhbGciOi)" -- . ':!*.md' ':!operacao'` e `git log -p -S "SUPABASE_SERVICE_ROLE_KEY=" --oneline`.
- Resultado: nenhuma chave real. Todas as ocorrências são comentários/código citando os nomes das variáveis e prefixos (ex.: `whsec_` como prefixo esperado em `auth-send-email`/`resend-webhook`, "service_role" em comentários de migration). A única ocorrência do padrão de busca no histórico é a própria linha do `auditor.md` que descreve a checagem.

## 4. Consistência de deploy
- Conferido: as 34 funções em `supabase/functions/*/` (exceto `_shared`) contra as entradas de `supabase/config.toml` e a tabela de `supabase/functions/DEPLOY.md`, comparando `verify_jwt` nos três lugares e exigindo prova de identidade na terceira coluna do DEPLOY.md para toda função `--no-verify-jwt`.
- Resultado: sem diferenças. 34/34 funções com entrada em `config.toml`, 34/34 com linha em `DEPLOY.md`, 0 divergências de `verify_jwt` entre os dois arquivos, 0 funções `--no-verify-jwt` sem prova na terceira coluna.

## 5. Partida (POST vazio, sem sessão)
- Conferido: `POST {}` com `apikey` mas sem JWT/segredo em cada uma das 34 funções.
- Resultado: 32 responderam 400/401 (o esperado para função protegida sem credencial). Nenhum 503/BOOT_ERROR — nenhuma função quebrada no boot.
- `whatsapp-webhook` respondeu 200 (`{"received":true}`) em vez de 400/401/405 — verificado no código (`supabase/functions/whatsapp-webhook/index.ts`): é deliberado. A função sempre confirma 200 rápido para a Meta não reenviar o mesmo evento; a autenticidade (assinatura `X-Hub-Signature-256`, segredo por profissional) é conferida **depois**, por `phone_number_id`, antes de qualquer gravação — um POST sem assinatura válida não escreve nada, só recebe o 200 de ack. Não é diferença sem explicação; não abri incidente.
- `confirmar-autorizacao` e `google-oauth-callback` responderam 400 (esperado: `state`/token ausente é a própria prova de identidade dessas duas).

## 6. Migrations
- Conferido: última migration em `supabase/migrations/` é `0110_op_origens.sql` (cria `public.op_origens_de_cadastro()`). Chamei a ação `funil` do op-agente, que usa essa função via RPC.
- Resultado: a função existe e respondeu sem erro (`origens: [{"origem":"desconhecida","contas":5,"assinantes":2,"cortesias":0}]`) — evidência indireta de que 0110 está aplicada.
- Limitação registrada: isso confirma só a última migration, pela existência do objeto que ela cria; não é uma verificação de que *todas* as 110 migrations foram aplicadas na ordem certa. O `resumo` do op-agente não expõe essa informação.

## Incidentes abertos por esta auditoria
- #7 · aviso · auditor · "1 conta ativa sem mp_preapproval_id" — ver item 2.
