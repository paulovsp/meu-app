# Dependências — 2026-09-14

**Veredito: 🔴 VERMELHO** — dois prazos abaixo de 60 dias (um deles praticamente em cima) e pacotes críticos (Expo SDK, React Native, expo-updates) com versão maior nova, 3 ciclos de SDK atrás.

`npm audit` não rodou: `node_modules` não existe nesta sessão.

## Pacotes (`package.json`)

Comparado com `npm view <pacote> version` hoje. "Muda o runtime" marca `expo-*`, `react-native*`, `@react-native-*` e pacotes com código nativo — atualizar esses exige build novo, não OTA (regra do repositório, `runtimeVersion` por fingerprint).

| o que | versão atual | versão nova | muda o runtime? | risco | até quando | o que fazer |
|---|---|---|---|---|---|---|
| `expo` | 54.0.37 | 57.0.22 | **sim** | **alto** — SDK 54 (set/2025) recebe correções críticas só até ~set/out 2026, segundo a política de ~1 ano de suporte do próprio Expo; hoje é 14/09/2026, ou seja a janela está fechando agora ou já fechou | ~set/out 2026 (não confirmado com precisão — ver "Expo SDK" abaixo) | Planejar upgrade para SDK 57 (ou a versão suportada na hora), com build novo nas duas lojas. Não é patch, não faz sozinho. |
| `react-native` | 0.81.5 | 0.87.1 | **sim** | alto — atrelado 1:1 ao Expo SDK; sobe junto com o `expo` acima, nunca sozinho | junto com o item acima | Mesmo upgrade do SDK. |
| `expo-updates` | 29.0.20 | 57.0.22 | **sim** | alto — é o próprio mecanismo de fingerprint/OTA; descompasso de 3 SDKs | junto com o item acima | Mesmo upgrade do SDK. |
| `expo-asset`, `expo-audio`, `expo-clipboard`, `expo-constants`, `expo-document-picker`, `expo-file-system`, `expo-font`, `expo-image-manipulator`, `expo-image-picker`, `expo-linear-gradient`, `expo-local-authentication`, `expo-notifications`, `expo-print`, `expo-secure-store`, `expo-sharing`, `expo-splash-screen` | cada um travado no SDK 54 | todos realinhados ao SDK 57 | **sim** | alto (mesma causa) | junto com o item acima | `npx expo install --fix` depois de decidir subir de SDK; nunca pacote a pacote. |
| `expo-text-extractor` | 2.0.0 | 2.0.0 | sim (nativo, OCR) | — | — | Sem atualização disponível; nada a fazer. |
| `@react-native-async-storage/async-storage` | 2.2.0 | 3.1.1 | **sim** | médio — versão maior nova | sem prazo anunciado | Avaliar changelog do 3.x isoladamente; não é urgente, mas é versão maior. |
| `react-native-gesture-handler` | 2.28.0 | 3.3.0 | **sim** | médio — versão maior nova | sem prazo anunciado | Avaliar changelog do 3.x; costuma exigir ajustes de API. |
| `react-native-webview` | 13.15.0 | 14.0.1 | **sim** | médio — versão maior nova | sem prazo anunciado | Avaliar changelog do 14.x. |
| `react-native-safe-area-context`, `react-native-screens`, `react-native-svg` | 5.6.2 / 4.16.0 / 15.12.1 | 5.9.1 / 4.28.0 / 15.15.5 | **sim** | baixo — atualização menor, mas nativa | sem prazo | Só junto do upgrade do SDK (mais seguro testar tudo de uma vez). |
| `react-native-notify-kit`, `react-native-signature-canvas` | 10.7.0 / 5.1.0 | 10.7.1 / 5.1.1 | **sim** (nome bate no padrão `react-native-*` da regra do repositório) | baixo | sem prazo | Tecnicamente é patch, mas a regra do repositório trata qualquer `react-native-*` como pacote nativo — não entra no PR automático de JS puro. Fazer junto do upgrade do SDK. |
| `@supabase/supabase-js` | 2.109.0 | 2.116.0 | não (JS puro) | baixo | sem prazo | É atualização **menor**, não patch — fora do critério de PR automático desta rotina. Dá pra fazer manualmente quando quiser; sem risco de fingerprint. |
| `react` | 19.1.0 | 19.3.0 | não (JS puro), mas pareado com a versão de `react-native` que o SDK espera | baixo, mas não mexer sozinho | sem prazo | Não é patch (é menor) e não deve subir isolado do `react-native` — deixar para o upgrade de SDK. |
| `@expo/vector-icons`, `@react-navigation/native`, `@react-navigation/native-stack`, `base64-arraybuffer`, `react-native-calendars`, `react-native-url-polyfill` | — | — | — | — | — | Já na versão mais nova do npm (o lockfile já pegou o topo do range). Nada a fazer. |
| `eslint`, `eslint-config-expo`, `jest`, `jest-expo`, `typescript`, `@babel/eslint-parser`, `@expo/ngrok` | — | — | não (`devDependencies`, não entram no build nem no fingerprint) | baixo | sem prazo | Todas têm versão maior nova (ex.: `typescript` 5.9→7.0, `jest` 29→30). Só ferramenta de desenvolvimento; atualizar quando der, sem pressa e sem risco de runtime. `jest-expo` deve ficar pareado com a versão do `expo` quando o SDK subir. |

Nenhuma atualização qualifica como **patch de pacote JavaScript puro** esta semana (a única que seria patch dentro do range, `react-navigation/native`, já está na versão mais nova travada no lockfile). Por isso não há branch nem PR nesta rodada.

## Expo SDK

- Em uso: **SDK 54** (setembro/2025). Atual: **SDK 57** (lançado ~30/06/2026, segundo o changelog oficial) — o app está 3 ciclos de SDK atrás.
- Política de suporte do Expo: cada SDK recebe correções críticas por ~1 ano a partir do lançamento. Para o SDK 54 isso aponta para **setembro/outubro de 2026** — ou seja, agora. Não encontrei uma data exata publicada (só "até o próximo SDK" / "~1 ano"); vale confirmar direto em https://docs.expo.dev/versions/v54.0.0/ ou no changelog antes de decidir o cronograma do upgrade.
- Fonte: [Expo SDK 57 — changelog](https://expo.dev/changelog/sdk-57), [Expo SDK reference](https://docs.expo.dev/versions/latest/).

## Google Play

- Prazo de 31/08/2026: apps novos e atualizações precisam mirar Android 16 (API 36); apps existentes precisam de API 35 para continuar disponíveis a novos usuários em Android mais recente. Já passou (estamos em 14/09).
- **O app já está em conformidade**: o `targetSdkVersion` padrão do Expo SDK 54 é **36** (confirmado em docs.expo.dev), acima do exigido. Nenhum override em `app.json`/`expo-build-properties` reduzindo isso. Nada a fazer aqui — mas o upgrade de SDK acima também mantém isso em dia.
- Fonte: [Target API level requirements — Play Console Help](https://support.google.com/googleplay/android-developer/answer/11926878), [Meet Google Play's target API level requirement — Android Developers](https://developer.android.com/google/play/requirements/target-sdk).

## Fornecedores

| fornecedor | aviso | data | o que fazer |
|---|---|---|---|
| **Supabase** | Tabelas do schema `public` deixam de ser expostas automaticamente à Data API — vai exigir `GRANT` explícito por tabela | **30/10/2026** (46 dias) | O repo já tem várias migrations de grant explícito (`0006_grants.sql`, `0011_...`, `0038_...`, `0053_...`). Provável que já esteja coberto, mas **confirmar antes de 30/10** que toda tabela usada pelo app tem grant — senão o app para de ler/escrever sem aviso. |
| Supabase | Suporte a Postgres 14 termina | 01/07/2026 — já passou | Só relevante se o projeto ainda estiver no PG 14; checar no dashboard. Sem sinal disso no repositório. |
| Supabase | Bibliotecas cliente (`supabase-js` etc.) derrubam Node 20 | 30/06/2026 — já passou | As Edge Functions do Dr.Sig rodam em Deno, não Node — baixo impacto. |
| Mercado Pago | Nada de descontinuação de `preapproval`/`payments` encontrado nesta busca | — | Sem ação. Reconferir na próxima rodada. |
| AssemblyAI | O modelo em uso, `universal-3-5-pro`, **não** está descontinuado — é o modelo atual recomendado. (Quem foi descontinuado foi o `universal-3-pro`, que não usamos.) | — | Sem ação. |
| DeepSeek | `deepseek-v4-flash` (modelo em uso) foi aposentado, mas o nome antigo continua funcionando por compatibilidade, roteado automaticamente para o V4.1 Flash | sem prazo de corte anunciado | Sem urgência — funciona hoje. Quando convier, trocar o nome do modelo para `deepseek-flash` (novo nome oficial do V4.1 Flash). Isso é troca de nome de modelo do mesmo provedor, não troca de provedor — mas fica registrado para o dono decidir, não é feito sozinho aqui. |

## Os três mais urgentes

1. **Expo SDK 54 perto do fim das correções críticas** (~set/out 2026, ou seja, agora) — app 3 SDKs atrás do atual (57); upgrade exige build novo nas duas lojas, não é OTA.
2. **Supabase: grants explícitos por tabela até 30/10/2026** — repo já parece ter os grants, mas precisa confirmar antes da data ou o app para de acessar dados sem aviso.
3. **`react-native`, `expo-updates` e o resto do SDK com versão maior nova** — mesma causa do item 1; sobem juntos, num upgrade só, nunca pacote a pacote.

Nenhum PR aberto nesta rodada (sem atualização patch de JS puro qualificando).
