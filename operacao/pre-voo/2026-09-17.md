# Pré-voo · 2026-09-17 · modo `ota`

Primeiro pré-voo depois do lançamento. Referência: build 24 (1.0.0), commit
`2b6dadb`, publicada na produção do Google Play a 100 % (status `completed`
pela API do Play em 17/09). Runtime instalado nos aparelhos:
`d80c4d17344acc54822cf2e1d1e9fcb355556ce9`.

## Veredito: **voa** (OTA)

| # | item | | evidência |
|---|---|---|---|
| 1 | Working tree | ✅ | `git status --short` vazio; `origin/main..HEAD` vazio (antes da correção do item 4 — ver abaixo) |
| 2 | Testes e lint | ✅ | `npx jest --silent`: 200/200 passando · `npx eslint src App.js index.js`: 0 erros (61 avisos, todos pré-existentes) |
| 3 | Migrations | ✅ | `supabase migration list --linked`: 0001–0110 iguais local e remoto; as 8 novas desde a build 24 (0103–0110) já estão aplicadas |
| 4 | Funções | ✅* | 34 pastas = 34 entradas em `config.toml` = 34 publicadas (`functions list`), `verify_jwt` batendo nas três pontas. `DEPLOY.md` não tinha `assinatura-cancelar` e `cotacao-atualizar` — acrescentadas neste pré-voo (ambas `padrão`, JWT do app, como já estão publicadas). **Nenhuma função precisa de deploy**: as 33 foram publicadas em 11/09 11:47 UTC, depois do último commit que as tocou (`149bb9a`, 11:43 UTC); `op-agente` publicada em 16/09 04:19 UTC, depois de `8af733c` (04:15 UTC). Os `_shared` alterados desde então (`publicarMeta.ts`, `playInstalacoes.ts`) só são importados pela `op-agente`. |
| 5 | Fingerprint | ✅ | `expo-updates fingerprint:generate` → `d80c4d17…56ce9` = `runtimeVersion` da build 24 (`eas build:list`). **OTA serve.** `versionCode` 24 em `app.json` = build 24 (não muda em OTA). |
| 6 | Páginas | ✅ | 13/13 páginas de `docs/` em https://app.drsig.com.br/ respondendo 200 |
| 7 | Notas da versão | ✅ | abaixo |

\* A correção do `DEPLOY.md` foi commitada junto com este relatório; o
working tree volta a ficar limpo com o push.

## O que vai no OTA (só JavaScript)

Commits que tocam `src/` desde a build 24:

- `b0c04f9` — avisos da Início (check-in, atraso, transcrição) voltam a aparecer: esperam o guia de primeira entrada, disparam ao voltar do segundo plano, fila pendurada não trava a próxima.
- `994d0c6` — cancelar compromisso futuro pergunta se cobra; agenda ordena recorrentes e remarcados juntos, pelo relógio.
- `149bb9a` — cadastro pergunta "como você conheceu o Dr.Sig?" (opcional).
- `4c5fe00` — texto da recarga no Perfil (recarga só em cartão).

O resto do log é operação (agentes, skills, rondas, Edge Functions já publicadas) e não entra no bundle.

## Notas da versão

```
<pt-BR>
Correções e melhorias:

• Os avisos da tela inicial (check-in, atraso e transcrição pronta) voltam a aparecer — inclusive ao retornar de outro aplicativo.
• Ao cancelar um compromisso futuro, o app pergunta se a sessão será cobrada.
• A agenda agora ordena horários fixos e remarcados juntos, pela hora.
• No cadastro, você pode contar como conheceu o Dr.Sig (opcional).
• Recarga de créditos de IA agora é por cartão.
</pt-BR>
```
(392 caracteres)

## Ordem de publicação

1. Deploys de função: **nenhum**.
2. `db push`: **nenhum** (0110 já aplicada).
3. Comando final, a rodar pelo dono:

```
npx eas-cli update --channel production --message "Avisos da Início de volta; cancelar futuro pergunta se cobra; agenda ordena recorrentes e remarcados; origem no cadastro; recarga em cartão"
```

Depois do `eas update`, conferir que o update saiu no runtime
`d80c4d17344acc54822cf2e1d1e9fcb355556ce9` (o comando imprime; se aparecer
outro hash, o update foi para um runtime que ninguém tem — ver AGENTS.md
sobre `scripts` no `package.json`).
